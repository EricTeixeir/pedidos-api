openapi: 3.0.0
info:
  title: Order API
  version: 1.0.0

servers:
  - url: http://localhost:3000

paths:
  /order:
    post:
      summary: Create order
      responses:
        '201':
          description: Order created